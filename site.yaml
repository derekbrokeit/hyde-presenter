mode: development
media_root: media # Relative path from content folder.
media_url: /media # URL where the media files are served from.
base_url: / # The base url for autogenerated links.
plugins:
    - hyde.ext.plugins.meta.MetaPlugin
    - hyde.ext.plugins.auto_extend.AutoExtendPlugin
    - hyde.ext.plugins.syntext.SyntextPlugin
    - hyde.ext.plugins.textlinks.TextlinksPlugin
    - hyde_ext.presenter.PresenterPlugin
context:
    data:
        tweet_via: scicalculator
meta:
    nodemeta: meta.yaml
    created: !!timestamp 2010-01-01 00:00:00
    author: Derek Ashley Thomas
    charset: utf-8

presenter:
    # settings for the presenter plugin
    # These settings can also be set per resource. Plus, each element
    # can be customized with it's own yaml-matter
    default:
        # this shows the default behavior, null values can be left out
        element: div
        meta: null # meta data are html tag elements
        slides:
            filter: null      # filter used to parse each section, defaults to None
            element: div
            meta: null
    impress: # presentation id 'impress'
        element: div
        slides:
            filter: markdown
            element: div
            meta: { class: 'step slide-%d' } # example meta data

